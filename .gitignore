# Design reference screenshots (local only)
assets/badge-hero-*.png
assets/critique-*.png
assets/fixed-canvas-*.png
assets/simplified-*.png
assets/hero-*.png
screenshots/

# NCAC brand reference — kept local, too heavy for git. The canonical spec
# (docs/brand/NCAC-V1-BRAND.md) IS tracked; these are the 14MB source PDF, the
# 43 rendered pages, and the logo/font/old-brand binaries (~149MB total).
# Regenerate the pages from the PDF with PyMuPDF if they're ever needed.
docs/brand/NCAC-Style-Guide.pdf
docs/brand/style-guide-pages/
docs/brand/resources/

# Local environment files
.env
.env.*
!.env.example

# Local machine/tooling cache
.DS_Store
.superpowers/
__pycache__/

# Generated reports / logs / backups (local only)
*.log
**/*.bak-before-*
scripts/dedupe_report.json
scripts/place_photos_report.json
