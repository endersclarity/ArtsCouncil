---
target: v1-discovery-map county-polish re-score 3 (fresh independent critique)
total_score: 32
p0_count: 0
p1_count: 0
timestamp: 2026-07-02
slug: cultural-map-redesign-stitch-lab-v1-discovery-map
---
# Critique: v1-discovery-map county-polish re-score 3 (2026-07-02, live at localhost:4180)

## Score: 32/40 (Good)
Heuristics: status 3, real-world 3, control 3, consistency 3, error-prevention 3, recognition 3, flexibility 4, minimalist 4, recovery 3, help 3.

> Revision note (same session): an initially-filed P1 — "selecting an event erases every other event diamond (46 → 1 rendered)" — failed re-verification and is **retracted as a rig artifact** (see Environmental caveats). Score adjusted 31 → 32 with its removal.

Judged fresh in real Chrome (hidden-tab rescue rig, own tab, `?contract=critique4`), desktop 1448px. Exercised: first paint; all four mode tabs; mood chips + search "theatre" combined with an active "Art" chip (3-of-195 scoping, "Show 5 more matches outside your filters" hatch, expanded to 8); events list selection (Night Dimension, Karaoke) AND map-diamond selection (synthetic canvas click on `event-points` → African Drum & Dance, correct card + URL); Places list selection (Nevada Theatre) AND ordinary map-dot selection (non-featured "Drop by Drop A River is Formed" via `place-density` click); every event card line checked against `data/events.json`; discovery rail census (19 cards under Everything, 8 under Events lens, tab colors/uppercase computed); a trail selection (Alan Thiesen Trail, traced red geometry + facts card, links audited); County view with a card open (map re-fit to zoom 10.7, card stays, chip stays); one full Drift cycle (start → stop 1 North Star House → auto-advance stop 2 Curious Forge → stop 3 Empire Mine → wheel-scroll "Paused — you have the wheel" → ▶ resume (stop 4 Center for the Arts) → Escape exit, bar removed, body class restored). Brand tokens verified live: `--ncac-red #ff2500`, `--ink #000000`, `--paper`/`--surface #ffffff`, Polymath variable on body and headings, links computed `rgb(255,37,0)`, rail program tabs `rgb(255,37,0)` field + white type. Console clean — the only error logged all session was my own probe querying a nonexistent layer id.

## Environmental caveats (not scored)
- Hidden tab at ~0 fps; rAF shimmed to setTimeout and MapLibre pumped via `triggerRepaint` interval. Motion smoothness, GSAP entrance quality, and Drift flight/orbit character NOT judged. Camera flights complete but crawl; two CDP screenshot timeouts and one extension disconnect were recovered by restarting the repaint pump.
- Rescue rig replaced the basemap with OpenFreeMap Liberty: basemap tint and the faint OSM path-context layer's weight not judged.
- Lazyloaded images painted blank in several screenshots (event card hero, ASiF placeholder); `img.complete`/`naturalWidth` checked where it mattered. First-paint image impressions not judged.
- Window resize is a no-op on hidden tabs — mobile judged from CSS only: real `max-width: 880px` (hamburger nav sheet, control panel → bottom sheet 52%, drawer restack, drag handle) and `520px` blocks, plus `pointer: coarse` and two `prefers-reduced-motion` blocks. Unverified live.
- **`queryRenderedFeatures` on symbol layers is unreliable in the throttled tab.** An initially-filed P1 claimed selecting an event erased all sibling diamonds (`event-points` rendered 46 → 1). Re-verification with a healthy repaint pump shows 46 rendered with the card open — identical to the no-selection baseline (the 48→46 delta is symbol collision of co-located diamonds, present in both states). Mechanism: `event-points` is the app's only symbol layer on app data; in a 0-fps tab, symbol placement doesn't re-run after `setData` until a camera change, while circle layers (places) need no placement — which is why places never showed the effect. Finding retracted; do not trust symbol-layer `queryRenderedFeatures` in this rig without pumping repaints and nudging the camera first.

## Anti-patterns
Not slop. The system is brand-native and confident: white/black/gray ground, one red, Polymath everywhere, Card=Poster anatomy (red program tab, framed key image, black venue bar, meta), red-field knockout event cards, quiet unframed map, red genuinely reserved for frame/selection/links. The marquee provenance problem from the last pass is verifiably fixed: the third-party Karaoke card now closes with "From the **Nevada County Arts Council** calendar" — honest, not "Presented by." What drags the score is a persistent collision cluster in the drawer's top-right corner and three state-sync gaps that survived the last critique.

## Priority issues (most damaging first)
- [P2] **Top-right collision cluster on every right-side card.** (a) "‹ ALL ROUTES" overlaps long route titles — "Makers / Working Artists" renders "…Working Arti⟨sts⟩" under the button (title right edge 1239 vs button left 1141, vertical overlap 128–144, screenshot). (b) The trail card title "Alan Thiesen Trail" runs under its CLOSE button. (c) The County view chip (z-index 9) now floats **on top of** every open card/drawer (z-index 8), overlapping the image corner of event cards and the Close/back controls (chip rect 1185,94 × 103×33 vs Close 1177,117; overlap verified via rects + `elementFromPoint`). Three different fixtures fight for one corner at default desktop width.
- [P2] **Route-stop tap: three signals, two stories** (persists from rescore 2). Tapping stop "ASiF Studios" on the Makers route opens the place card and flips the active mode tab to **Places**, while the URL stays `mode=paths&path=makers-working-artists` and the left panel still says "Route selected · 3 numbered stops." Reload restores a different surface than the one on screen.
- [P2] **Trails mode never reaches the URL** (persists). In Trails with Alan Thiesen Trail selected, the URL reads `mode=places` with no trail param — the only mode whose deep link restores the wrong surface (places/events/paths all sync).
- [P2] **"Show 5 more matches outside your filters" silently destroys the filter** (persists). Clicking it removed the "Art ✕" chip entirely (verified absent; `intent=Art` dropped from URL). The copy promises a peek; the behavior is deletion, with no "filter cleared" cue and no one-tap undo.
- [P2] **Search claims a map scope it doesn't have.** With "theatre" active the count line says "3 matching of 195 places **on the map**," but the map is unchanged: all ambient density dots keep rendering and all 18 labeled featured markers (Broad Street Inn, Elixart, BrewHaus…) stay up with no match highlight. The list filters; the map shrugs; the copy says otherwise.
- [P2] **"Tonight" kicker generated for daytime events.** The Wonder Docent exhibition (data: 11:00–18:00) renders "Tonight / 11am—6pm" — a UI-composed label, not feed text. Karaoke (6–9pm) is legitimately "Tonight"; the gallery show closed at 6pm and isn't a tonight plan. Any today-event gets "Tonight" regardless of hours.
- [P2] **"Events" is triple-booked.** Mode tab "Events" (48 upcoming events), mood chip "Events 74" (places that host events), and rail lens chip "Events" (8 cards) — one word, three cohorts, three counts on the same screen. The mood chip especially reads as a broken shortcut to the mode tab.
- [P2] **The design team still narrates a visitor surface** (persists). Makers route blurb: "This path shows that the Discovery Map can point to culture being made, taught, shared…" — pitch-deck register in shipped visitor copy; the other three routes speak to the walker.
- [P3] Map→list selection is one-way: after a diamond click the correct card opens but no `.event-list-row` carries any active state (list-click does highlight). No stale highlight this pass — just silence.
- [P3] "CANDIDATE IMAGE" black badge on the ASiF placeholder card — internal QA vocabulary on a visitor surface; the card's own "Good to know: Placeholder photo — final image coming." already says it honestly.
- [P3] Redundancy set: venue bar repeats the name inside itself ("Nevada Theatre, Nevada City" left, "Nevada Theatre" right); list rows state the category twice (meta "Galleries & Studios / Nevada City" + badge "Galleries & Studios"); the trail card links the same BYLT URL three times ("Trail info: BYLT", "WEBSITE Open website", red "Visit site" button).
- [P3] All-caps microlabels everywhere (top nav, "WHAT ARE YOU IN THE MOOD FOR?", "EVENTS (22 · 48 DATES)", "WALK THIS ROUTE", "BEFORE OR AFTER", rail program tabs via `text-transform`) against the guide's explicit no-all-caps rule; trail spec labels (DIFFICULTY/LENGTH/SURFACE/USES) additionally set in red, spending the reserved color on non-link, non-selection chrome.
- [P3] Empty search is a dead end: 'No places match "zzzqqq".' — no clear-search action, no suggestion.
- [P3] Anchor monograms (HI, MK, ST, FD, GA) still undecoded at point of use in path stop lists; the decoded label exists only on full cards ("Working artist studio stop").
- [P3] Mode tabs are plain buttons — no `role=tablist`/`aria-selected` (2 aria-live regions and empty-alt rail thumbs are otherwise correct); page has an 8px vertical scroll (scrollHeight 696 vs 688) that nudges the map stage.
- [P3] Booktown Books stop hook is a template fragment: "Shops & Makers stop for living like a local."

## Personas
Jordan (weekend visitor): filters to Art, types "theatre," gets an honest "3 matching of 195" and a hatch that quietly deletes her Art mood when she peeks. Opens tonight's exhibition card — "Tonight / 11am—6pm" — and nearly plans an evening around a gallery that closes at 6. Local parent: Events list is clean and human ("Jul 2 · STEAM Exploration Saturdays · 2 dates"), taps a diamond, the right flyer opens — but the list beside it never highlights which event that was. Diana (arts council): the karaoke card now says "From the Nevada County Arts Council calendar" — her meeting-stopper from last month is gone; her new wince is "Makers / Working Arti" clipped under a button on the demo projector, and a black CANDIDATE IMAGE tag. Casey (30-second skeptic): presses ▶ Drift, the county tours itself with real photos and credits, a scroll takes the wheel, Esc exits clean — Casey still has nothing sarcastic to say.

## Strengths
- **The prior P1 is fixed and verified live:** third-party events close with "From the Nevada County Arts Council calendar," not "Presented by" — checked on the Karaoke card against `events.json` (`presenter: "Nevada County Arts Council"` rendered as provenance, not authorship).
- **Card contents check out against the data.** The Night Dimension card's venue ("Wonder Docent, Nevada City"), recurrence ("Also Jul 3, Jul 4, Jul 5 +10 more" = 14 dates), times (11:00–18:00 → 11am—6pm), image URL, and blurb all match the record; African Drum & Dance renders guide-perfect "Tue, Jul 7 / 4—5pm" with "Also Jul 14" and a Family friendly chip.
- **Search + filter scoping is honest and generous:** cohort-aware counts ("3 matching of 195"), an explicit out-of-filter hatch, and intersection semantics that behave predictably.
- **Drift's state machine is exemplary:** auto-advance, wheel-take with plain language ("Paused — you have the wheel"), resume, Escape; aria-live stop announcements ("Now at Empire Mine, stop 3 of 42"); County chip and rail correctly cleared during the tour and restored after.
- **Trail flow ships real facts:** difficulty/length/surface/uses grid, regulation line ("Foot and bike only. Dogs on leash."), BYLT source, traced OSM geometry with trailhead marker, cultural nearbys stitched to the trailhead.
- **Brand execution is measured, not claimed:** `#ff2500` everywhere it should be, cool neutrals, Polymath on body and display, Card=Poster rail anatomy, red-field knockout event card, red only for frame/selection/links (selected place = red ring + red label chip; selected trail = red trace), quiet unframed map stage.
- **Honesty affordances:** "Map pin is approximate — double-check the address before you go"; photo credits ("Cullen328, via Wikimedia Commons (CC BY-SA 4.0)", "via Google"); "Placeholder photo — final image coming."
- **Zero application console errors** across every exercised flow.

## Questions
- Who owns the top-right corner of the drawer? County chip, CLOSE, "‹ ALL ROUTES," and long titles all claim it — is there a stacking/reserved-space contract?
- Should "Show N more matches outside your filters" peek (highlight extras, keep the chip) or clear? If clear is intended, say "Filters cleared" and offer undo.
- "Tonight" for any today-event: is the kicker meant to be a date badge ("Today") or a plan ("Tonight")? The word choice decides whether daytime events read wrong.
- Is the mood chip "Events 74" earning its slot, given the Events tab exists four centimeters above it?

## Data flags (do not fix in UI — flag to data owner)
- **Duplicate venue records:** `places.json` carries both "Nevada Theater" (Performing Arts, `nevada-theater-nevada-city`, 39.2633/-121.02089) and "Nevada Theatre" (Historic Places, `nevada-theatre-nevada-city`, 39.2628/-121.0201) — the same 401 Broad St landmark ~60m apart. The Drop by Drop card's Nearby list offers "Nevada Theater" while the featured anchor is "Nevada Theatre"; two dots for one theatre.
- **Malformed place name:** "The Lyric Rose Rose Theatre Company" (`the-lyric-rose-rose-theatre-company-nevada-city`) — doubled "Rose," reads broken in every list it appears in.
- Trumba prose ships stale/rule-breaking copy verbatim: the Night Dimension description still advertises the June 20 opening reception (past), has the ".,": "August 16th.," typo, ordinals against brand date rules, and internal title drift ("Those **We** Meet" title vs "Those **You** Meet" in body).
- Events feed diversity: 4 of 8 events-lens rail cards are one venue's "Youth Maker XD Camp Experience" series (The Curious Forge) — distinct events, no dedupe applies, but one venue owns half the marquee.
- Trails gaps: "Big Boca OHV Tie Trail" / "Big Boca OHV Trail" ship with no difficulty or length; several trails carry the county-level town fallback ("Nevada County").
- `events.json` sets `presenter: "Nevada County Arts Council"` on third-party events (Karaoke at GVBC); the UI now renders it honestly as calendar provenance, but the field itself is still misleading for any future consumer.
