---
phase: 01-design-visual-refresh
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - website/cultural-map-redesign/index-maplibre.css
  - website/cultural-map-redesign/index-maplibre-hero-intent.css
autonomous: true

must_haves:
  truths:
    - "All card variants (cat-card, explore-cat-card, muse-card, event cards, detail-event-card, intent-strip-card) use the shared card tokens for radius, shadow, and hover"
    - "Hero section reads as intentional editorial layout with clean whitespace and strong typography"
    - "Map container has minimal or no border treatment (not 4px neumorphic frame)"
    - "Section intro headings and tooltips use the editorial shadow/palette consistently"
  artifacts:
    - path: "website/cultural-map-redesign/index-maplibre.css"
      provides: "Card styles, map section, tooltips, detail panel, and explore list updated with editorial tokens"
      contains: "var(--card-radius)"
    - path: "website/cultural-map-redesign/index-maplibre-hero-intent.css"
      provides: "Hero section refined with editorial treatment"
      contains: "var(--card-"
  key_links:
    - from: "index-maplibre.css card styles"
      to: ":root card tokens from Plan 01"
      via: "CSS variable references"
      pattern: "var\\(--card-"
    - from: "index-maplibre-hero-intent.css hero styles"
      to: ":root palette tokens from Plan 01"
      via: "CSS variable references"
      pattern: "var\\(--ink\\)|var\\(--cream\\)"
---

<objective>
Apply the new editorial design tokens (from Plan 01) across all card components, the hero section, map container, tooltips, detail panel, and explore list. This is the section-by-section CSS refactoring pass that makes the token changes visible across the entire site.

Purpose: Plan 01 updated the foundation tokens. This plan propagates those tokens into every visual section, ensuring consistent editorial styling. Cards get shared tokens, the hero gets cleaner treatment, and the neumorphic map border is softened.

Output: Updated section styles in index-maplibre.css, refined hero in index-maplibre-hero-intent.css
</objective>

<execution_context>
@C:/Users/ender/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ender/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-visual-refresh/01-RESEARCH.md
@.planning/phases/01-design-visual-refresh/01-01-SUMMARY.md
@website/cultural-map-redesign/index-maplibre.css
@website/cultural-map-redesign/index-maplibre-hero-intent.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply card tokens and editorial styling across all CSS sections</name>
  <files>website/cultural-map-redesign/index-maplibre.css</files>
  <action>
Work through the CSS file section-by-section (per research line map). For each section, apply the new card tokens and editorial styling. DO NOT rename any CSS classes. DO NOT change grid/flex layouts — only update visual properties (colors, shadows, borders, border-radius, backgrounds).

**Section 3 — Hero (lines ~54-154):**
- Replace any neumorphic shadow references with `var(--shadow-raised)` or `var(--shadow-soft)`
- Background should reference `var(--cream)` or `var(--neuro-bg)`

**Section 5 — Category Cards (lines ~279-402):**
- `.cat-card`: Apply `border-radius: var(--card-radius)`, `box-shadow: var(--card-shadow)`, `background: var(--card-bg)`, `border: var(--card-border)`, `transition: var(--card-transition)`
- `.cat-card:hover`: Apply `box-shadow: var(--card-shadow-hover)`, add subtle `transform: translateY(-2px)`

**Section 6 — Map section (lines ~404-1012):**
- `.map-outer` or map container: Reduce border from 4px to `1px solid rgba(26,22,18,0.08)` or remove border entirely. Reduce border-radius to 8px or remove. Replace neumorphic inset shadow with `var(--shadow-soft)` or none.
- Map control overrides: ensure buttons use editorial shadows

**Section 7 — Tooltips (lines ~1014-1134):**
- Apply `border-radius: var(--card-radius)`, `box-shadow: var(--card-shadow)`, clean white background

**Section 8 — Detail panel (lines ~1136-1472):**
- Panel background: clean white
- Event cards inside detail: Apply card tokens
- Section borders/dividers: subtle 1px lines

**Section 9 — MUSE editorial cards (lines ~1474-1685):**
- `.muse-card`: Apply card tokens (radius, shadow, border, hover)

**Section 10 — MUSE showcase (lines ~1687-2176):**
- Story cards: Apply card tokens
- Reader panel: Clean white background, editorial shadows

**Section 11 — Explore list (lines ~2178-2574):**
- `.explore-cat-card`: Apply card tokens
- List items: Clean borders, subtle hover states

**Section 13 — Experience selector (lines ~2650-3200):**
- Experience cards: Apply card tokens

**Section 14 — Corridor panel (lines ~3209-3660):**
- Panel styling: Clean white, editorial shadows, card tokens for stop cards

**General rules for all sections:**
- Replace any hardcoded neumorphic shadow values with the `var(--shadow-*)` variables
- Replace any hardcoded `#F4EFE6` or similar parchment colors with `var(--cream)` or `var(--neuro-bg)`
- Replace any hardcoded `#4A6B7C` with `var(--ink)`
- Keep all existing responsive behavior, z-index values, transitions, and GSAP integration
- If a section already uses CSS variables correctly, leave it alone
  </action>
  <verify>
1. Grep for hardcoded neumorphic shadow patterns: `grep -n "6px 6px 12px\|4px 4px 8px\|inset 3px 3px" index-maplibre.css` — should return 0 results outside comments
2. Grep for old parchment hex: `grep -n "#F4EFE6\|#E8DFD0" index-maplibre.css` — should only appear in :root definitions or comments, not in section styles
3. Grep for card token usage: `grep -c "var(--card-" index-maplibre.css` — should be 20+ references
4. Load site locally and check that cards, tooltips, detail panel, explore list all render with consistent styling
  </verify>
  <done>
All card variants use shared card tokens. Map container has minimal border. Tooltips, detail panel, explore list, MUSE cards, experience cards, and corridor panel all use editorial shadow/palette. No hardcoded neumorphic shadows remain outside :root.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refine hero section editorial treatment in hero-intent.css</name>
  <files>website/cultural-map-redesign/index-maplibre-hero-intent.css</files>
  <action>
The hero-intent variant already has a modern feel (frosted glass mast bar, tabbed discovery panel). Refine it to feel more intentionally editorial:

**Mast bar (.mast-inner):**
- Keep the frosted glass effect (backdrop-filter: blur) — it already works well
- Ensure background uses clean white tones, not parchment
- Verify border uses subtle `rgba(26,22,18,0.08)` not a heavier value

**Hero subtitle (.hero-variant-sub):**
- Increase letter-spacing slightly for kicker/tagline feel
- Ensure font-family is var(--font-mono) for the monospace label aesthetic

**Discovery panel (the tabbed section below the hero):**
- Tab bar: Clean, minimal. Active tab should have a clear indicator (underline or background change, not neumorphic raised effect)
- Tab panels: Apply card tokens for any card-like elements inside
- Intent cards (.intent-strip-card): Apply `border-radius: var(--card-radius)`, `box-shadow: var(--card-shadow)`, `background: var(--card-bg)`
- Editorial cards in hero: Same card token treatment
- Increase whitespace (padding) in the hero area if it feels cramped

**Feature picks / curated routes / events tabs:**
- Cards within these tabs: consistent card token application
- Clean borders between items
- Ensure hover states use `var(--card-shadow-hover)` and subtle lift

**General:**
- Remove any remaining neumorphic shadow references
- Replace any hardcoded old palette colors with CSS variables
- Ensure `!important` usage is minimized — only where truly needed for override specificity
- Test that the hero-intent.css overrides work correctly with the updated base CSS from Task 1
  </action>
  <verify>
1. Grep for hardcoded shadows in hero-intent.css: `grep -n "6px 6px\|4px 4px\|inset 3px" index-maplibre-hero-intent.css` — should return 0
2. Grep for card token usage: `grep -c "var(--card-" index-maplibre-hero-intent.css` — should be 5+ references
3. Load the hero-intent variant locally and verify:
   - Mast bar is clean and readable
   - Tabs work and active state is clear
   - Cards have consistent editorial shadow/radius
   - Whitespace feels generous, not cramped
  </verify>
  <done>
Hero section has a polished editorial treatment with consistent card tokens, clean typography, generous whitespace, and no neumorphic shadows. The hero reads as an intentional editorial layout, not a default landing page.
  </done>
</task>

</tasks>

<verification>
1. Load the hero-intent variant locally and scroll through every section
2. All cards (category, explore, MUSE, events, experience, corridor) should have visually consistent border/shadow treatment
3. Hero section should feel clean and editorial
4. Map container should not have a heavy neumorphic frame
5. No visual regressions — all sections render correctly, no broken layouts
</verification>

<success_criteria>
- Zero hardcoded neumorphic shadow patterns outside :root (replaced by variable references)
- Card token variables used across 8+ card component types
- Hero section reads as intentional editorial layout
- Map container border reduced from neumorphic to minimal/none
- Site loads without CSS errors or broken layouts
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-visual-refresh/01-02-SUMMARY.md`
</output>
