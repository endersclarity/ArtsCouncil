---
phase: 09.1-directory-audit-fixes
plan: "01"
subsystem: ui
tags: [security, xss, vanilla-js, es5, explore, directory]

# Dependency graph
requires:
  - phase: 09-directory-page-redesign
    provides: explore-view.js and explore-controller.js modules with the directory hub explore section
provides:
  - XSS-safe HTML rendering in explore-view.js via escapeHTML on all data interpolations
  - Wired .directory-back-btn click handler in explore-controller.js
  - directoryHeaderArea cleared on exploreSetCategory(null)
  - Removed dead onCitySelect parameter from buildCityFilterPills signature and caller
affects: [09.1-directory-audit-fixes]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "escapeHTML fallback pattern: var escapeHTML = (window.CulturalMapCoreUtils && window.CulturalMapCoreUtils.escapeHTML) || inline-impl"
    - "Back button wiring after headerEl.innerHTML assignment via querySelector('.directory-back-btn')"

key-files:
  created: []
  modified:
    - website/cultural-map-redesign-stitch-lab/index-maplibre-explore-view.js
    - website/cultural-map-redesign-stitch-lab/index-maplibre-explore-controller.js

key-decisions:
  - "escapeHTML declared at top of explore-view.js IIFE with fallback implementation — works even if core-utils loads after"
  - "City values in data-city HTML attributes are also escaped (defense in depth, even though data is controlled)"
  - "desc already has HTML tag stripping via replace(/<[^>]*>/g,'') — escapeHTML adds entity escaping on top"
  - "onCitySelect removed from both signature and caller — city pill clicks wired via DOM delegation in controller"
  - "directoryHeaderArea cleared with innerHTML='' before null check guard (safe: element always exists on hub page)"

patterns-established:
  - "XSS prevention: all user-data values interpolated into innerHTML must be wrapped with escapeHTML()"
  - "Back button wiring: wire dynamic buttons immediately after headerEl.innerHTML = html assignment"
  - "State reset: clear derived DOM state (stale headers) in the same else-branch that clears model state"

requirements-completed: [B1, B3, S3, S4, S9]

# Metrics
duration: 8min
completed: 2026-02-18
---

# Phase 09.1 Plan 01: XSS Fix, Back Button, Header Clear Summary

**escapeHTML applied to all 8 data interpolation points in explore-view.js; back button wired to exploreSetCategory(null); stale category header cleared on reset; dead onCitySelect param removed from view and controller**

## Performance

- **Duration:** 8 min
- **Started:** 2026-02-18T00:00:00Z
- **Completed:** 2026-02-18T00:08:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- B1 (security): All user-data strings (asset.n, asset.c, asset.l_original, asset.d, category name, city name) now escaped via escapeHTML before innerHTML injection in explore-view.js
- B3 (UX blocker): The .directory-back-btn in the expanded category header now fires exploreSetCategory(null) — clicking back actually works
- S9 (correctness): directoryHeaderArea.innerHTML is cleared when exploreSetCategory(null) is called, preventing stale headers from persisting in the DOM
- S3/S4 (code quality): onCitySelect parameter removed from buildCityFilterPills signature and its caller — dead code eliminated

## Task Commits

Each task was committed atomically:

1. **Task 1: XSS fix + onCitySelect removal in explore-view.js** - `68a0d30` (fix)
2. **Task 2: Back button wiring + header clear + dead param removal in explore-controller.js** - `0e3e2fe` (fix)

## Files Created/Modified
- `website/cultural-map-redesign-stitch-lab/index-maplibre-explore-view.js` - Added escapeHTML reference at top of IIFE; wrapped all 8 data interpolation points; removed onCitySelect from buildCityFilterPills signature; escaped city values in data-city attributes
- `website/cultural-map-redesign-stitch-lab/index-maplibre-explore-controller.js` - Wired .directory-back-btn click handler after headerEl.innerHTML assignment; added directoryHeaderArea.innerHTML='' in null-cat else branch; removed onCitySelect: null from buildCityFilterPills call

## Decisions Made
- escapeHTML declared with fallback implementation at top of view IIFE — works whether or not core-utils.js has loaded at point-of-use
- city values escaped in both data-city attribute and button text content (defense in depth)
- desc already strips HTML tags via regex — escapeHTML adds entity escaping on top for full protection
- directoryHeaderArea guarded with `if (headerArea)` null check per research guidance (core element but defensive is free)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plan 02 of 09.1 addresses the remaining blockers: B2 (null guards in bindings.js), B4 (mobile map toggle), and should-fix items S1-S14
- explore-view.js and explore-controller.js are now clean and correct — no follow-up work needed on these files

## Self-Check

- `68a0d30` exists in git log: FOUND
- `0e3e2fe` exists in git log: FOUND
- `index-maplibre-explore-view.js` modified: FOUND (9 escapeHTML occurrences)
- `index-maplibre-explore-controller.js` modified: FOUND (back button wiring at line 136-142)
- Zero `onCitySelect` occurrences in both files: CONFIRMED

## Self-Check: PASSED

---
*Phase: 09.1-directory-audit-fixes*
*Completed: 2026-02-18*
